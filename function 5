#include <stdio.h>
int program();
int main() {
   
 program();
}
int program()
{
     int age;
   printf("enter your age=");
   scanf("%d",&age);
   if(age>=18)
   {
     printf(" eligible to vote ");
   }
   else
   {
       printf("not eligible");
   }
return 0;
}
