#include <stdio.h>
int add();
int main() {
     int c,d;
     printf("enter a number :");
    scanf("%d%d=",&c,&d);
    add(c,d);
   
}
int add(int a,int b){
     int add=a+b;
    printf("add= %d",add);
}
